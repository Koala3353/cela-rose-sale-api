// Bootstrap entry: ensure environment variables are loaded before other modules
import 'dotenv/config';

// Import the main server file (which expects env to be loaded)
import './index';

export {};
